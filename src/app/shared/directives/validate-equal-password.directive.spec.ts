import { ValidateEqualPasswordDirective } from './validate-equal-password.directive';

describe('ValidateEqualPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateEqualPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
