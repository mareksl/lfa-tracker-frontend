import { PercentageColorDirective } from './percentage-color.directive';

describe('PercentageColorDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentageColorDirective();
    expect(directive).toBeTruthy();
  });
});
